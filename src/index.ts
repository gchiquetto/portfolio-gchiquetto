import { styled } from './styles'

export const Container = styled('div', {
  background: '$gray900',
  minHeight: '100vh',
  padding: '3rem',
  display: 'flex',
  gap: '2rem',
})

export const Content = styled('div', {})
